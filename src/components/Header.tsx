import Nav from "./Nav";

const Header: React.FC = () => {
  return (
    <div id="header" className="flex-center">
      <p id="title">testing</p>
      <Nav />
    </div>
  );
};

export default Header;
